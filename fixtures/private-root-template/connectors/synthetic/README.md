# Synthetic private connector

This inert template exercises manifest discovery. Copy the template outside the public checkout, rename `open-outdoor.private.example.json` to `open-outdoor.private.json`, and set `OUTDOOR_PRIVATE_ROOT` to that absolute directory. Generated output remains below that external root.
