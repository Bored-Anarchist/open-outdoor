# WP-004 evidence — External private-root composition spike

Implemented absolute-path and containment validation, manifest discovery, a synthetic connector template, and catalog composition whose output path is always beneath the external root. The root and public checkout may not contain one another.

Acceptance command: `pnpm test:private-root`. The test rejects the repository-local template as an active root, composes from an OS temporary external root, and verifies the private classification and output location. Normal `pnpm quality` does not require `OUTDOOR_PRIVATE_ROOT`.

Local result (2026-08-19): external composition, repository-root rejection, connector traversal rejection, and no-private-root public operation all passed.
