# WP-107 evidence

**Status:** Accepted by project owner on 2026-08-30

The versioned container uses Node's vetted AES-256-GCM and scrypt implementations, authenticates the routing header, encrypts inventory and schema metadata, validates counts and attachment hashes in staging, supports current-plus-previous private schema, and calls the commit callback only after full success. Wrong key, tamper, and truncation tests prove no partial mutation.
