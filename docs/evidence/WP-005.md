# WP-005 evidence — Private downstream workflow spike

Added an inert private-repository workflow template with explicit dispatch, read-only permissions, no privileged pull-request event, no shared cache, no artifact upload, runner-temporary paths, and unconditional cleanup. A policy checker rejects boundary regressions. The PowerShell compatibility test uses isolated temporary Git repositories to fetch public `main` while proving a synthetic private marker never appears in the public tree.

Acceptance command: `pnpm test:private-downstream`.

Local result (2026-08-19): workflow policy and temporary public-upstream/private-downstream Git compatibility tests passed. A real private repository remains operator-owned and is not created by this public package.
