# WP-009 evidence — Phase 0 evidence and budget review

**Status:** Gate review complete; Phase 0 passed

Published the evidence index, current binding `iphone14-ios26.6-phase0-v1` release profile, strict Phase 0 gate schema v2, deterministic gate evaluator, bounded hosted-CI ledger/schema/evaluator, risk/decision review, hosted-CI usage report, and the Phase 0 gate report. The original iOS 26.2 launch records remain historical evidence; the active profile advanced after the instrumented report and Settings both confirmed that the reference phone now runs iOS 26.6.

Windows verification proves that the gate record is schema-valid and that its declared `passed` state matches zero computed blockers. T-REL-001-C09 confirms blocker reporting; C10 confirms the evaluator passes only when every work package, prerequisite budget, and clean window is accepted. T-REL-004-C11/C12 prove the bounded-first-20 and avoidable-failure behavior.

The bounded hosted-CI window passed on 2026-08-23 with 20/20 applicable runs and zero avoidable failures. WP-008 was accepted by the project owner based on its existing physical and automated evidence, with unexecuted backup-inventory, locked/pre-first-unlock, complete spool/replacement, and downgrade cases retained as explicit residual risks rather than test passes. WP-007's required background screen-lock collection, recovery, Stop behavior, active-spool protection class, acknowledgement/durability budgets, and 30-minute memory smoke pass on the current iPhone 14/iOS 26.6 profile. `BUD-REC-001` passed with Start p95 13 ms and Stop p95 2 ms; `BUD-REC-002` observed zero committed-data gap across process death; and `BUD-MEM-001` passed with 392 samples over 1,953.73 seconds and independently recomputed p95 resident memory of 42.141 MiB. Measured battery/thermal acceptance remains deferred to WP-307/WP-503 and makes no current claim. Phase 0 therefore passes, and WP-101/Phase 1 may be scheduled subject to ordinary package readiness.
