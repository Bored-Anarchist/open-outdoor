# WP-009 evidence — Phase 0 evidence and budget review

**Status:** Gate review complete; Phase 1 blocked

Published the evidence index, binding `iphone14-ios26.2-phase0-v1` release profile, strict Phase 0 gate schema v2, deterministic gate evaluator, bounded hosted-CI ledger/schema/evaluator, risk/decision review, hosted-CI usage report, and the Phase 0 gate report.

Windows verification proves that the gate record is schema-valid and that its declared `blocked` state matches the computed blockers. T-REL-001-C09 confirms the current physical/budget/CI-window blockers are reported; C10 confirms the evaluator passes only when every work package, prerequisite budget, and clean window is accepted. T-REL-004-C11/C12 prove the bounded-first-20 and avoidable-failure behavior.

The bounded hosted-CI window passed on 2026-08-23 with 20/20 applicable runs and zero avoidable failures. WP-008 was subsequently accepted by the project owner based on its existing physical and automated evidence, with unexecuted backup-inventory, locked/pre-first-unlock, complete spool/replacement, and downgrade cases retained as explicit residual risks rather than test passes. WP-007 process-death sequencing and `BUD-REC-002` subsequently passed with sequence 2 preserved across relaunch, zero observed committed-data gap, and monotonic continuation through sequence 5. The remaining blockers are WP-007's other physical cases, the acknowledgement budget, and the 30-minute memory budget. Measured battery/thermal acceptance is deferred to WP-307/WP-503 and makes no current claim. Accordingly, the repository remains a technical prototype and WP-101/Phase 1 must not be scheduled as accepted work.
