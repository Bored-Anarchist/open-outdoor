# WP-502 evidence

**Status:** Implementation and automated verification complete. Physical testing is deferred to the end of Phase 5 by the owner's 2026-09-10 instruction, recorded in ADR-049; it is not marked passed.

Native accessibility preferences are observed at startup and on changes, with stale-read protection and subscription cleanup. Increase Contrast takes precedence over a lower-contrast override. Product text supports Dynamic Type and Bold Text; controls expose focus/busy/selected/expanded/disabled states and suppress duplicate asynchronous actions. Queued, deduplicated iOS announcements report status changes without announcing every metric update. Existing deliberate discard confirmation and 52-point controls are retained.

Four mock-native tests cover changing settings, initial-read races, announcement deduplication and async control semantics. Browser acceptance includes twelve axe-core 4.13.0 WCAG A/AA scans with all component states expanded, a negative-control audit, and existing keyboard/target/large-text/state/reduced-motion checks. Automated scans pass; incomplete/manual checks are retained in the generated report. These tests do not establish physical VoiceOver or outdoor readability.

Validation: full repository quality, privacy/boundary checks, governance links, Python regressions, the production-quality runner and iOS JavaScript export. The combined suite has 329 tests in 49 files; iOS export contains 596 modules. See [production quality](../PRODUCTION_QUALITY.md) for exact commands, the 99-case physical matrix, case IDs and final acceptance procedure.

All fixtures are synthetic and new code is project-authored with AI assistance. axe-core (MPL-2.0), React and React Test Renderer (MIT) are development-only dependencies. No new native dependency or source-data right is introduced. WP-506 must pass the deferred physical matrix before production acceptance.
