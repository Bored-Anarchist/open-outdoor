# WP-405 evidence

**Status:** Accepted on 2026-09-09 by Bored-Anarchist. See the [Phase 4 acceptance disposition](../PHASE_4_GATE_REPORT.md).

T-E2E-003-C01 covers stable core/SDK version negotiation, canonical/capability compatibility, private identity, complete package version/file locks, SHA-256 integrity, provenance, and external-root isolation. Tampered/unlocked packages, traversal/junctions, arbitrary hooks, incompatible versions, and malformed manifests fail before package execution. A pinned synthetic normalizer produces the same private result under current and compatible proposed contracts; an incompatible proposed core fails.

The explicit private CLI verifies the shipped external-root example and suppresses private filesystem/document details in diagnostics. The local Git downstream test incorporates a public update while retaining its private marker and follows the actual source branch. The private workflow template replaces its placeholder with verification and a clean public build, restricts refs to exact public-main commits, and keeps private material in ephemeral storage. The template is policy/YAML validated; it was not dispatched against a real private repository.

Version-1 composition remains unchanged. See [Private extension guide](../PRIVATE_EXTENSION_GUIDE.md) and [Connector ecosystem](../CONNECTOR_ECOSYSTEM.md).
