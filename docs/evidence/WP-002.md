# WP-002 evidence — Toolchain and monorepo bootstrap

Implemented exact Node, pnpm, Python, uv, Expo, React Native, GIS compatibility, iOS, identity, region, and schema pins in schema-validated release configuration. Added pnpm workspaces for mobile, browser fixture, shared, data, privacy, and configuration packages; strict TypeScript, Prettier, Vitest, deterministic public fixtures, a uv Python shell, Windows bootstrap, and routed/cancelled/timeout-bounded CI.

Local acceptance commands: `pnpm install --frozen-lockfile`, `pnpm quality`, `pnpm build:web`, and `uv run --frozen python -m unittest discover -s python/tests`.

Local result (2026-08-19): frozen pnpm install passed its 513-entry supply-chain policy check; strict TypeScript passed; 13 tests passed; release/workflow schemas passed; the browser fixture and synthetic public catalog built; Python 3.13.14 created from the frozen uv lock and its unit test passed. Hosted Windows execution begins when the change is pushed.
