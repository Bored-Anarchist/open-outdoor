# WP-202 evidence

**Status:** Implemented; local acceptance suite passed on 2026-08-30

Ingestion now applies per-asset failure isolation, abortable time limits, payload and archive bounds, compression-ratio checks, path/link rejection, secret redaction, structured quarantine reasons, and content-addressed immutable raw storage. Public raw roots reject private, organization-private, and restricted classifications. Root checks require explicit absolute locations, disjoint public/private trees, and a private root outside the public checkout.

The malicious suite covers traversal with relative and drive-qualified paths, links, entry/expanded-size exhaustion, compression bombs, secrets, and public/private boundary violations. This verifies `REQ-SRC-003` and `REQ-SEC-001` for the connector framework. Archive decoding adapters delivered later must provide entry metadata to this boundary before extraction.

Verification: strict TypeScript build and `packages/data` Vitest suites.
