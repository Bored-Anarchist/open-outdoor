# WP-204 evidence

**Status:** Implemented; local acceptance suite passed on 2026-08-30

Entity resolution now provides deterministic type-specific blocking and explainable, versioned scores for places, trails, temporal condition/restriction records, and media. Initial thresholds are recorded in `DEFAULT_RESOLUTION_THRESHOLDS`; below-threshold candidates remain reviewable or rejected rather than silently merged.

Append-only audit events cover link, merge, split, tombstone, and explicit reversal. Replaying active events derives catalog remaps and tombstones while retaining the original decisions and reasons. UUID and remap validation prevents ID recycling and malformed audit references.

The labelled synthetic corpus reaches precision `1.0` and recall `1.0` across the four entity families, exceeding the `0.95` fixture gate. These figures are fixture acceptance evidence, not a New York production-accuracy claim; WP-207 must add source-specific labels before activation. This verifies `REQ-SRC-004` at the canonical processing boundary and accepts the initial rule/threshold choice in `ADR-023`.

Verification: strict TypeScript build and `packages/data` Vitest suites.
