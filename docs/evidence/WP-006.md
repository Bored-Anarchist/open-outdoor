# WP-006 evidence — iOS build and Windows sideload feasibility

Accepted ADR-037 and ADR-020. Added a manual-only `macos-26` workflow that selects Xcode 26.4.1 exactly, builds without signing access, packages an unsigned IPA with SHA-256, and retains it for one day. Added Windows AltServer signing/install/refresh instructions and distinct channel identities.

Local result (2026-08-19): Expo reports all SDK dependencies up to date; workflow syntax and exact release configuration pass. The macOS candidate job has not been dispatched. Physical iPhone launch, exact provisioning expiry, refresh, and relaunch are pending and cannot be claimed from Windows or CI alone. See `docs/IOS_SIDELOAD_FEASIBILITY.md`.

Additional Windows-only result: Metro successfully produced the iOS Hermes bundle from 569 modules. The generated bundle was 1,570,423 bytes with SHA-256 `A8388C897AFFA848C3839A7F468C5A95576B59BC39760B53E83A930A637C21A5`; export metadata SHA-256 was `2A16CF16122465FB52BFD0E4D234D202B0E22DA13A3C660AE19668439CF8CC17`. Expo correctly declined native iOS project generation on Windows, so Xcode project generation and compilation remain assigned to the pinned hosted macOS job. These hashes describe ignored local evidence, not a release artifact.

Hosted-runner availability check (2026-08-19): GitHub's current `macos-26` Arm64 inventory lists Xcode 26.4.1 build 17E202 at `/Applications/Xcode_26.4.1.app` and its iOS 26.4 SDK. This verifies that the workflow's exact toolchain selection is presently satisfiable, but it is not a substitute for a successful project build.
