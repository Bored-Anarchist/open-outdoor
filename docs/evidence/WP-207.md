# WP-207 evidence

**Status:** Implemented connector foundation; local acceptance suite passed on 2026-08-31

The active source registry targets NYS DEC roads, trails, and OPEN-NY points of interest plus USDA Forest Service MVUM roads. ArcGIS and Socrata discovery enumerate complete deterministic pages. Canonical trail/place output retains per-field provenance and source attribution; POIs map into the project taxonomy while preserving unmapped raw values.

Road features also emit independent access directives. Public-use denial becomes closed, seasonal designations become restricted, and mapped public use becomes open with an explicit warning that designation does not guarantee passability. Existing versioned entity resolution generates duplicate candidates without silently merging records.

`T-INT-003-C01` through `C04` cover rights, normalization, taxonomy, paginated emission, access output, dedup candidates, validation, checksums, and coverage.

Verification: strict TypeScript build and `packages/data/test/new-york.test.ts`.
