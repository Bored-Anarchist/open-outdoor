# WP-003 evidence — Privacy classification, diagnostics, and leak gates

Implemented classification and diagnostic schemas, recursive secret/identifier/location redaction, 24-hour default and 72-hour maximum retention, publication-path/content scanning, fixture approval metadata, deliberate generated leak fixtures, a dedicated CI context, and incident response.

Acceptance command: `pnpm test:privacy`. Tests inject secret, precise-location, private-classification, and prohibited-path candidates into temporary directories and require publication to fail.

Local result (2026-08-19): all diagnostics and deliberate publication-leak cases passed, and a scan of the complete public checkout reported no boundary violation. Hosted status-context execution begins when the change is pushed.
