# WP-303 evidence

**Status:** Resumable catalog activation coordinator implemented; local acceptance passed on 2026-09-01

The activation coordinator performs the exact free-space preflight before staging, copies in resumable bounded chunks, verifies the staged catalog checksum, delegates channel-bound signature and anti-replay verification to the catalog trust boundary, enforces app/catalog compatibility, validates remaps and promotion links, and uses compare-and-swap pointer activation.

Every pre-switch interruption retains the previous active catalog and durable staging offset. A pointer switch is rolled back when first launch is interrupted or fails; a rolled-back candidate can be safely retried. The accepted anti-replay version advances only after successful first-launch confirmation. A protected private-data digest is checked across staging, validation, switching, rollback, and completion.

The supplemental WP-303 suite exercises every `T-INT-002-C01`–`C08` boundary, resume behavior, checksum/trust/channel/space failures, first-launch rollback, safe retry, and private-data mutation detection. The original WP-008 compatibility and exact 9 GiB maximum-profile formula tests remain binding.

Verification: strict TypeScript build and the WP-303 Vitest suite.
