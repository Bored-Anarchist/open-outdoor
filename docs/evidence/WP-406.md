# WP-406 evidence

**Status:** Implemented; automated acceptance passed on 2026-09-09.

T-INT-003-C13 covers per-source metrics, required-field coverage, nested schema/volume drift, staleness and rights-review alerts, persisted failure circuits, explicit recovery, and reviewed schema fingerprints. Monitored connector emission and source checkpoints are deferred until validation passes; failed runs retain the last-good snapshot. State and output are written as one synced atomic replacement, and private operations cannot target a public store.

Tests restart a file-backed store, reject corrupt state, quarantine failed sources without stopping healthy jobs, time out a hanging emitter, preserve output on checkpoint failure, and update freshness alerts without acquisition. Diagnostics retain reason codes rather than raw private exceptions. Generated source contracts now exercise the health policy. This is a single-worker local operations contract; callers retain responsibility for isolated/idempotent staging and public-pack publication gates.

See [Connector ecosystem](../CONNECTOR_ECOSYSTEM.md) for operator recovery steps and boundary rules.
