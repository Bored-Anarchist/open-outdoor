# WP-007 evidence — minimal native tracker and durability spike

**Status:** Implementation complete; physical iPhone acceptance pending

Implemented an autolinked Expo iOS module containing a Swift `CLLocationManager`/`CMAltimeter` tracker spike. During an explicit active session it writes newline-delimited, monotonically sequenced observations to `Library/Application Support/Tracking/Active`, synchronizes every observation, applies `completeUntilFirstUserAuthentication`, and excludes the directory, manifest, and spool from system backup. A durable `active-session.json` identifies session/mode/spool without adding per-sample manifest writes. Relaunch inspection derives the highest contiguous sequence from synchronized JSONL, ignores only an undecodable torn final line, and exposes inspect, recover, and discard controls through main-queue Expo module functions.

The shared replay contract accepts out-of-order batches deterministically, ignores identical duplicates, rejects conflicting duplicates, and reports sequence gaps rather than inventing samples. The native implementation remains energy-conscious by running sensors only for active recording, requiring explicit High Accuracy, applying lower-frequency Balanced/Endurance filters, and avoiding continuous polling. No battery-life result is claimed.

Windows-verifiable evidence:

- T-PHY-001-C01 through C06 contract prechecks pass for contiguous/out-of-order replay, duplicate idempotence, conflict rejection, gap reporting, and invalid state.
- `tools/validate-native-spikes.mjs` confirms native autolinking, main-queue recovery/readback controls, durable manifest/spool policy, torn-line handling, barometer/location sources, and the energy-conscious implementation constraints.
- Release configuration binds the current profile `iphone14-ios26.6-phase0-v1`, 30-second/one-batch durability, 500 ms acknowledgement, 150 MiB memory, and the deferred energy policy.

Candidate re-verification on 2026-08-23 passed the pinned Windows frozen install, TypeScript, formatting, release/workflow/native validators, 48 Vitest cases, browser production build, and four Python tests. [macOS run 32650388170](https://github.com/Bored-Anarchist/open-outdoor/actions/runs/32650388170) compiled and packaged the recovery implementation at exact commit `8f46b4f6397cc547eae9f4c45dc48deb57ad5a38` with Xcode 26.4.1 build 17E202. The downloaded 7,251,244-byte IPA matched the published SHA-256 `D60D377B25432BEFEEB51D3C59802BB715B950A66CA4883B76DB2142C7690653`. This proves the exact native candidate builds; it does not substitute for the remaining physical-device cases.

## Physical-device evidence

Physical startup attempt on 2026-08-23: the operator successfully installed the candidate and confirmed the app title, but the app content was an empty white screen. This is recorded as a failed launch, not acceptance. Commit `9848d8bb3ca451803ab326e907ec7d9b4ce2330b` replaces eager native-module loading with a guarded capability check, visible startup diagnostics, disabled native controls when unavailable, and a root render error boundary. [Replacement macOS run 32653754917](https://github.com/Bored-Anarchist/open-outdoor/actions/runs/32653754917) passed with Xcode 26.4.1 build 17E202 and autolinked `OpenOutdoorNativeSpikes`; the downloaded 7,252,654-byte IPA has bundle ID `org.openoutdoor.local` and SHA-256 `DE7E791762DC945EA007ED6D9B83F10C5A74F751594D400F9AE00FE5C8E0D25F`. Physical retest of this replacement remains pending.

Physical diagnostic retest on 2026-08-23: the replacement rendered `Native startup check failed: OpenOutdoorNativeSpikes is not registered in this build`. This is a second failed launch and proves the JavaScript fallback worked while native runtime registration did not. Expo resolution reproduced the root cause: the workspace package was discovered and its pod linked, but Expo 56 discarded it from `ExpoModulesProvider` because the root-level podspec was not declared in `expo-module.config.json`. Commit `0ad06acb2f64521706095a65ddcb10cd672b4e20` declares the podspec and Swift product module and adds a macOS build gate that inspects the generated provider. [Corrected macOS run 32654962813](https://github.com/Bored-Anarchist/open-outdoor/actions/runs/32654962813) passed and logged registration of `OpenOutdoorNativeSpikesModule`; the downloaded 7,252,672-byte same-identity IPA has SHA-256 `2174FCC21B842E7AAA1FA15723DDA869E4894187E03D928F8ACFA251BB62B741`. Physical retest remains pending.

Corrected physical launch retest on 2026-08-23: after same-identity installation of the `0ad06ac` IPA, the operator reported `Native tracker ready for Phase 0 workoff`. This passes corrected candidate installation, JavaScript startup, Expo provider registration, and native module initialization. It does not pass the remaining tracker, interruption, memory, protection, backup, or A-to-B physical cases.

Functional tracker baseline on 2026-08-23: the operator reported that Always permission, Balanced start, two-minute locked-screen recording/inspection, explicit stop, and one-minute post-stop inspection all matched the expected states. The iOS screen recording failed, so exact UI messages and start/stop timing were not captured. This supports functional native-control, short screen-lock continuity, and no-unintended-session behavior, but it does not satisfy the 500 ms acknowledgement measurement.

Process-death recovery on 2026-08-23: the operator reported a recoverable spool containing nine valid observations, no `Torn final line ignored` warning, successful resume through `Recover interrupted session`, and successful explicit stop after the follow-up locked-screen interval. A controlled repeat then reported two observations through sequence 2 before forced termination, the same two observations through sequence 2 after relaunch, and monotonic continuation through sequence 5 after recovery and explicit stop. The committed-data gap observed across process death was zero, within the 30-second and one-native-batch limits. This is an operator-reported physical pass for `T-PHY-001-C08` and `BUD-REC-002`; it does not substitute for the permission, protection, or memory cases.

Endurance adversity on 2026-08-23: session `956AC747-A5E3-3E95-9A96-D55515B43474` reached three valid observations through sequence 3 before the screen-off Airplane Mode/Wi-Fi-off interval, retained exactly sequence 3 during the interrupted radio/GPS interval with no torn-line warning, and advanced monotonically to sequence 4 after connectivity restoration. Explicit Stop committed sequence 4, and inspection after a further one-minute screen-off interval reported no active or recoverable session. This is an operator-reported physical pass for `T-PHY-001-C07`; it also reconfirms the no-unintended-session portion of `T-PHY-001-C09` but does not pass its permission-loss/recovery portion.

Permission loss/recovery on 2026-08-23: the operator reported a High Accuracy session at eight observations through sequence 8 before changing location access to Never. After the permission-change and one-minute locked-screen interval, the same reported session remained coherent at 12 observations through sequence 12; after restoring Always with Precise Location it advanced monotonically to 22 observations through sequence 22. Explicit Stop committed sequence 23, and inspection after a further one-minute locked-screen interval reported no active or recoverable session. This is an operator-reported physical pass for `T-PHY-001-C09`. The four observations appearing around the permission transition were not timestamp-attributed, so this evidence does not claim immediate zero sampling after revocation; it proves coherent recovery, explicit durable Stop, and no lingering sensor session.

Instrumented physical-workoff candidate on 2026-08-23: commit `3a80859` adds a Phase-0-gated, coordinate-free report containing 20 bridge-inclusive Start/Stop acknowledgement samples, a native five-second resident-memory sampler with a strict 30-minute/20-sample minimum, and effective protection/backup-exclusion inspection for the active directory, manifest, and spool. Windows verification passed the full TypeScript build, formatting, release/workflow/native validators, 48 Vitest cases, browser production build, and five Python tests. [macOS run 32666689838](https://github.com/Bored-Anarchist/open-outdoor/actions/runs/32666689838) compiled and packaged the exact implementation with Xcode 26.4.1. The downloaded 7,291,574-byte IPA matched SHA-256 `01C60247CCFF7587032DD28BA749D709E8769024B5B1994F3A028F7920EDA9C9`.

Current-profile replacement on 2026-08-23: commit `90b4e67` advances the active report label and release profile to `iphone14-ios26.6-phase0-v1` without altering the diagnostic behavior or rewriting the original acknowledgement report. [macOS run 32667969398](https://github.com/Bored-Anarchist/open-outdoor/actions/runs/32667969398) compiled and packaged that exact commit with Xcode 26.4.1. The downloaded 7,291,573-byte IPA matched its published SHA-256 `F66EC88409A94FB00E0C223E768087489538233BBD98765F5C1DDDDF018347E8` and is the candidate for the remaining protection and memory workoff.

Acknowledgement benchmark on 2026-08-23: the exact coordinate-free [source report](artifacts/WP-007-ack-ios26.6-source.json) has SHA-256 `C6C7C399B0353A630FB644B18B16F4FABDE6D93A44CDAC123603870D88FEC748`, records iOS 26.6, and contains 20 Balanced Start plus 20 Stop samples. Independent nearest-rank recomputation produced Start p95 13 ms (max 25 ms) and Stop p95 2 ms (max 2 ms), both below the 500 ms threshold, with no coordinate/session/device-identifier field. The source report retains the candidate's stale `iphone14-ios26.2-phase0-v1` label while its runtime `systemVersion` is 26.6; the operator separately confirmed iOS 26.6 in Settings, so the active reference profile was advanced to `iphone14-ios26.6-phase0-v1` without rewriting the source evidence. This is a physical pass for `BUD-REC-001`.

Active-spool policy inspection on 2026-08-23: the iPhone 14/iOS 26.6 report declared `passed: true` and showed that the active directory, manifest, and spool all existed with effective `NSFileProtectionCompleteUntilFirstUserAuthentication` and backup exclusion. The private source attachment has SHA-256 `2A1B86F061040BF62F3E05BA3F140A36F0009076260F327376104AA688799EC1`; its session-specific spool UUID was replaced by `<session>` in the committed [redacted derivative](artifacts/WP-007-protection-ios26.6-redacted.json). This passes the effective-policy portion of the protection case. It does not prove locked-screen availability, sealed-library denial, or safe pre-first-unlock behavior.

Screen-off memory profile on 2026-08-23: the operator followed the instructed locked-phone interval on the current iPhone 14/iOS 26.6 profile. The report ran for 1,953.73 seconds with 392 samples at an approximately five-second cadence. Independent nearest-rank recomputation selected rank 373 and matched the reported p95 of 44,187,648 bytes (42.141 MiB); the maximum was 62,013,440 bytes (59.141 MiB), both below the 157,286,400-byte (150 MiB) threshold. The private source attachment has SHA-256 `7183CB8EAD990DFBC99C6BB910E973E1CD033EB63D1CD1699DD7190154243FDB`; the exact sample distribution is retained in the committed [redacted histogram](artifacts/WP-007-memory-ios26.6-redacted.json). This is a physical pass for `BUD-MEM-001`.

Still required on the iPhone 14 running iOS 26.6:

- locked-screen active-spool availability, sealed-library denial, and safe pre-first-unlock behavior.

Battery/thermal measurement is deferred to WP-307/WP-503. No browser, Windows replay, synthetic, or unexecuted physical result is claimed as acceptance.
