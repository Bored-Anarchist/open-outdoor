# WP-203 evidence

**Status:** Implemented; local acceptance suite passed on 2026-08-30

The canonical contract now includes TypeScript envelopes, runtime validation, JSON Schema 2020-12, and a SQLite migration at schema version `1.0.0`. It covers land units, places, trails, conditions, restrictions, observations, reviews, check-ins, and media. Validation enforces UUIDv4 identity, source identity, field provenance, rights/classification consistency, UTC instants, SI fields, vertical datum, explicit unknown/raw-enum behavior, EPSG:4326 axis/ranges, antimeridian normalization, closed/oriented/non-self-intersecting geometry, and typed geometry cardinality.

SQLite with R-tree is the accepted initial spatial staging equivalent under `ADR-016`: it uses the pinned Node runtime, needs no service, executes on Windows, and passes a deterministic 10,000-record grid insertion/spatial-query fixture. WP-206 and WP-209 retain a scale checkpoint against complete New York inputs.

The `0.9.0` to `1.0.0` compatibility migration preserves IDs and provenance; merge/split/retirement remaps cannot silently recycle IDs. This verifies `REQ-DAT-001` and the schema/provenance portion of `REQ-SRC-004`.

Verification: strict TypeScript build and `packages/data` Vitest suites, including execution of the committed SQL migration in SQLite.
