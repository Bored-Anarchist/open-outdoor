# WP-109 evidence

**Status:** Gate implemented; physical acceptance pending

The machine-readable Phase 1 gate and report fail closed on pending physical tracker correctness, representative 30-minute memory, VoiceOver, Dynamic Type, and controlled elevation evidence. Measured energy remains deferred to WP-307/WP-503 and is not reported as a Phase 1 failure or pass.

The persistent guided runner consolidates these checks into one redacted report. Repository ingestion independently validates thresholds and privacy before proposing evidence changes, and reviewer acceptance remains mandatory.

The 2026-08-30 report passed every tracker-correctness check and the 36-minute memory profile (436 samples, 84,066,304-byte p95) but failed elevation at 0 m and recorded the accessibility problem. Follow-up implementation adds independent barometer persistence, same-candidate targeted elevation/accessibility retries, ordered accessibility controls, real iPhone 14 hardware identification, embedded Git-commit binding, and sanctioned AltServer bundle-suffix validation. Because the native sampling implementation changed, final acceptance requires a fresh run on the replacement exact candidate rather than carrying measurements forward.
