# WP-504 evidence

Implementation: detached Ed25519 release manifests bind artifact checksums/sizes, complete file inventory, SBOM/DBOM/rights/notices roles, source commit, pinned materials, builder identity, channel and sequence. The verification CLI requires independently obtained expected provenance and active public keys and runs with Node built-ins only. Streaming hashes support large catalogs without loading whole artifacts into memory. Existing WP-010 catalog activation/replay verification remains separate and unchanged.

T-REL-002/003 coverage: a fresh verifier process succeeds on an ephemeral-key synthetic bundle; tampered artifacts, manifests, signatures and checksum indexes, extra files, wrong channel/sequence/commit/key, revoked/unknown keys, missing BOMs/materials, duplicate names and traversal are rejected. Test fixtures are synthetic and keys exist only in memory. The npm SBOM generator inventories 519 pinned dependencies including build tools. WP-302 retains data/source inventory generation; actual native/Python/tool inventory completeness and rights review are required per candidate.

Owner: Bored-Anarchist. Independent human reviewer: not recorded. Work branch: codex/wp-501, PR #10. Base: 370fd16f700f6f064c9ecf66990da0adae2161b5. Acceptance scope: implementation and automated synthetic verification, not production publication. Date: 2026-09-10. Requirements: REQ-REL-001, REQ-CFG-001, REQ-OSS-002. No user-data migration, UI or physical-performance impact.

Residual gates: production key provisioning/trust distribution, protected-commit review, clean-room reproducibility, advisory assessment, actual complete inventories, Apple signing, privacy/rights review and all WP-506 evidence. No signing secret, production artifact, release tag or trust root was created. See [release security](../RELEASE_SECURITY.md).

Validation result: pnpm quality passed (329 existing tests plus the release suite); the final expanded release suite passes 22 cases. pnpm quality:format, pnpm test:privacy (10 tests and public-boundary scan) and scripts/Test-Wp001.ps1 passed.
